# Week_12_Assignment

This notebook calls the NYT Top Stories API for the technology section, normalizes the JSON to a pandas DataFrame, and summarizes results by section and subsection. It returns 29 current items with titles, bylines, abstracts, URLs, and timestamps. I also export a CSV for reproducibility.
